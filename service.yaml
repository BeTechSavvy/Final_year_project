apiVersion: v1
kind: Service

metadata:
  name: flask-service
  labels:
    app: flask

spec:
  selector:
    app: flask

  type: NodePort

  ports:
  - name: http
    port: 5000
    targetPort: 5000
    nodePort: 30001